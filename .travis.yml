sudo: required
services:
  - docker

before_install:
  - docker build -t lashakitia/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run lashakitia/react-test npm run test -- --coverage

after_success:
  - docker build -t lashakitia/multi-client ./client
  - docker build -t lashakitia/multi-nginx ./nginx
  - docker build -t lashakitia/multi-server ./server
  - docker build -t lashakitia/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push lashakitia/multi-client
  - docker push lashakitia/multi-nginx
  - docker push lashakitia/multi-server
  - docker push lashakitia/multi-worker

deploy:
  provider: elasticbeanstalk
  region: "us-east-2"
  app: "multi-docker"
  env: "MultiDocker-env"
  bucket_name: "elasticbeanstalk-us-east-2-498638650819"
  bucket_path: "docker-multi"
  on:
    branch: master
  access_key_id: "$AWS_ACCESS_KEY"
  secret_access_key:
    secure: "$AWS_SECRET_KEY"
